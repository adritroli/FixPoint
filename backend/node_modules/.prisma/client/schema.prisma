generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

// Puedes ejecutar `npx prisma db pull` para importar el esquema real de la base de datos existente.
