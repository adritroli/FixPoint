import * as React from "react";
import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";

export default function HitosPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Hitos & Eventos</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Planificación de hitos y eventos clave.</p>
      </CardContent>
    </Card>
  );
}
