import DefaultLayout from "@/config/layout";

export default function PruebasPage() {
  return (
    <DefaultLayout>
      <div className="flex min-h-svh flex-col items-center justify-center">
        <h1 className="text-3xl font-bold">Pruebas Page</h1>
        <p className="text-lg">
          This is a simple example of a React component within the pruebas page.
        </p>
      </div>
    </DefaultLayout>
  );
}
