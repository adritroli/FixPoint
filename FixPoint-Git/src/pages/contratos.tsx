import * as React from "react";
import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";

export default function ContratosPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Contratos & Facturación</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Gestión de contratos y facturación.</p>
      </CardContent>
    </Card>
  );
}
