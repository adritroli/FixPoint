import * as React from "react";
import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";

export default function PlantillasPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Plantillas de Proyectos</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Plantillas para agilizar la creación de proyectos.</p>
      </CardContent>
    </Card>
  );
}
